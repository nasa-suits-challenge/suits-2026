# === Orchestrator API ===
# The README’s quick-start shows the API listening on 8787 (/health).
PORT=8787

# CORS for local dev (vite, unity, etc.). Adjust as needed.
CORS_ORIGIN=http://localhost:5173

# === Telemetry Stream Server (TSS) transport selection ===
# 'udp' for FY25 CAPCOM C server (binary/UDP); 'ws' for FY26 JSON/GeoJSON if provided.
# The branch README mentions both and tells you to choose here.
TSS_TRANSPORT=udp

# --- If using UDP (FY25 CAPCOM) ---
# Default CAPCOM port is 14141; server prints "...:14141" on startup and serves an HTTP UI on the same port.
TSS_HOST=127.0.0.1
TSS_PORT=14141
TSS_POLL_HZ=5          # how often to poll UDP in Hz (fast channels can be raised later)
TSS_TIMEOUT_MS=500     # UDP socket timeout

# --- If using WebSocket (FY26, if provided) ---
# Only used when TSS_TRANSPORT=ws
# TSS_WS_URL=ws://127.0.0.1:14141/telemetry

# Default EVA index for IMU queries via API proxy (example: /api/tss/imu?ev=1)
TSS_IMU_EV=1

# Optional: path to write/read record‑and‑replay logs for offline dev
REPLAY_FILE=

# Logging verbosity
LOG_LEVEL=info
